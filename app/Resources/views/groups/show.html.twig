{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ group.name }}</h1>

    <div class="row">
        <div class="col-sm-6 asdas">
            <div class="form-group">
                <label>{{ 'id'|trans }}</label>
                <input type="text" class="form-control" disabled="disabled" value="{{ group.id }}" />
            </div>

            <div class="form-group">
                <label>{{ 'group_name'|trans }}</label>
                <input type="text" class="form-control" disabled="disabled" value="{{ group.name }}" />
            </div>

            <a class="btn btn-default" href="{{ path('groups_index') }}">{{ 'back_to_list'|trans }}</a>
            <a class="btn btn-primary pull-right" href="{{ path('groups_edit', { 'id': group.id }) }}">{{ 'edit'|trans }}</a>

            {{ form_start(delete_form, {'attr': {'class' : 'inline'}}) }}
                <button class="btn btn-danger" type="submit">{{ 'delete'|trans }}</button>
            {{ form_end(delete_form) }}
        </div>
    </div>
{% endblock %}
